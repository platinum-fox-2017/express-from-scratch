<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm"
        crossorigin="anonymous">
</head>

<body>
    <%if (data.length === 0) { %>
        <h1 class="text-center">BELUM ADA DATA</h1>
        <% }else { %>
            <h1 class="text-center">
                <%=data[0].Subject.subject_name%>
            </h1>
            <div class="col-md-6 offset-md-3">
                <table class="table">
                    <thead>
                        <tr>
                            <th scope="col">NO</th>
                            <th scope="col">ID</th>
                            <th scope="col">Student Name</th>
                            <th scope="col">Score</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for (let i = 0; i < data.length; i++) { %>
                            <tr>
                                <td>
                                    <%= i+1 %>
                                </td>
                                <td>
                                    <%=data[i].Student.id %>
                                </td>
                                <td>
                                    <%=data[i].Student.first_name + ' ' + data[i].Student.last_name %>
                                </td>
                                <td>
                                    <% if (data[i].score === null) { %>
                                        <a href="/subjects/<%=data[i].Subject.id%>/<%=data[i].Student.id%>/give-score">Give Score</a>
                                        <% }else{ %>
                                            <%=data[i].score%>
                                                <%} %>
                                </td>
                            </tr>
                            <%} %>
                    </tbody>
                </table>
            </div>
            <% }%>
</body>

</html>