<!DOCTYPE html>
<html>
        <%include ./partial/head %>
<head>
    <title>Teacher</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
</head>
<body>
    <center>
    <h2> Teacher Data </h2>
    <h4> <a href='/teacher/add'>Add New Teacher</a></h4>
    <table class = 'table'>
        <tr>
            <th>ID</th>
            <th>First_name</th>
            <th>Last_name</th>
            <th>Email</th>
            <th>Subject</th>
            <th>Option </th>
        </tr>
          <% data.forEach(row => { %>
          <tr>
            <td><%= row.id        %></td>
            <td><%= row.first_name%></td>
            <td><%= row.last_name %></td>
            <td><%= row.email     %></td>
            <%if(row.id_subject == null){%>
                <td>unassigned</td>
            <%}else{%>    
                <td><%= row.subject.subject_name %></td>
            <%}%>
            <td>
                <a href='/teacher/edit/<%= row.id %>'>Edit</a> ||
                <a href='/teacher/delete/<%= row.id %>' onclick = "confirm('are you sure want to delete this student data?')">Delete</a>
            </td> 
          </tr>
          <% }) %>
        </table>
    </center>
</body>
</html>